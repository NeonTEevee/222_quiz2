#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <iostream>
int main ()
{
	int num, i, left, right, palin, len;
	char check[8];
	scanf("%d", &num);
	
	for(i=1; i<=num; i++)
	{
		len = strlen(atoi(num));
	}
}
